# Copy to .env and replace with your Supabase project values
# Copy into a new file named `.env` in the project root (DO NOT commit secrets)
# Replace the placeholders below with your Supabase project values.
# Example keys are not included for security.
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-public-anon-key
# Optional: a service role key for server-side operations (store securely)
SUPABASE_SERVICE_KEY=your-service-role-key

# Notes:
# - VITE_ prefixed env vars are exposed to the client by Vite. Do not put secrets
#   (like the service role key) in client-visible env files — store them server-side.
